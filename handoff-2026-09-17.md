# 핸드오프 — 2026-09-17 · 기준 커밋 `f50980e`

앞 문서: `handoff-2026-09-16-account-switch.md`(§9 까지). 09-15~17 작업이 **커밋 하나**(`f50980e`, 24 파일)로 나갔다.
★ 다음 세션 첫 문장: 「`f50980e` 기준. 규칙 게시·빌드가 됐는지부터 묻고, 안 됐으면 §3 순서대로. 됐으면 §4 실기기 확인 결과부터.」

---

## 1. 지금 상태

| 항목 | 값 |
|---|---|
| 기준 커밋 | `f50980e` (origin/main) — 직전 `35be613` |
| 검사 | 69개 · **초록 68 · 빨강 0 · 원본없음 1**(`togetherland-ui-mockup.html`) — 저장소 러너(2바퀴, `app/parts/` 까지 찾음) 기준 |
| CHECKS.md | 개정 18. ⚠️ 기준선 줄이 «초록 62 · 원본없음 7» 로 적혀 있다 — 프로젝트 사본 환경 숫자다. 다음 개정 때 위 값으로 고칠 것 |
| 규칙 파일 | `users/$userId/slots`(09-16) · `users/$userId/session`(09-17) 추가됨 — **게시 안 됨** |
| 빌드 | 09-15 이후 **안 나감**. 사용자 설치본은 그 전 판 |

---

## 2. 09-15~17 세션 지도 — 검사 69개의 출처

| 날짜 | 세션 | 한 일 | 검사 |
|---|---|---|---|
| 09-15 | 맥 ⑥ | sysinput-mac.js · 판정 키 번들 id · `displayNameOf` | (기존 sim-sysinput · sim-keysof-twin) |
| 09-15 | 맥 ⑦ + 트리아지 | electron 31→44 착수 · 제보 8건 A~D 분류(`bug-triage-2026-09-15.md`) | — |
| 09-15 | A 확인 + 날리기 설계 | 제보 7·8 코드 확인 · `handoff-chat-fly.md` | — |
| 09-16 | 날리기 구현 | 시안→구현, 320 px/s 로 5차 개정 | sim-chat-fly |
| 09-16 | 슬롯 동기화 · 방 채널 · 트리아지 1~6 | `users/{uid}/slots` + Storage 얼굴 · `recoverRoomChannel` · 런처 크기/자식창 테마/프리미엄 책상/아이템 회전/포커스 auth | sim-slot-sync · sim-room-channel · sim-cfg-fit · sim-child-theme · sim-premium-desk · sim-item-rot · sim-focus-auth |
| 09-16 | A·D·F-1·E | `ACCOUNT_LOCAL_KEYS`(올리고→검문→지움) · 무대 파츠 검사 · 렌더러 줌 한 통로 · `[활성]` 로그 | sim-account-switch · sim-pk-fit · sim-ui-zoom (+sim-google-login 고침) |
| 09-17 | C · E 추가 제보 | 휠→사다리 차단(`wheel:true`) | sim-wheel-kick |
| 09-17 | 이 세션 | §3 아래 | sim-admin-active · sim-room-order · sim-device-session (+sim-ui-zoom 고침) |

두 번 고쳐진 검사(sim-google-login · sim-ui-zoom)는 둘 다 «구조가 바뀌어 옛 판정이 거짓 빨강». 사유·해시는 CHECKS.md §3.

---

## 3. 09-17 이 세션에서 한 것

| 건 | 원인(코드) | 조치 | 파일 |
|---|---|---|---|
| **E-2** 관리자 권한 게임클라 앞에서 캐릭터는 포커싱인데 타이머 0 | node-window-manager 가 관리자 권한 프로세스의 `path` 를 못 읽어 `getActiveWindow` null → 폴링 일찍 return → 누적 tick 정지. 커서 이동 신호만 살아 캐릭터만 포커싱 | pid→`tasklist` 이름으로 판정(캐시·2초 상한). `[활성]` 줄에 `경로=없음(관리자권한)`. `[입력]` 1분 진단(등록 앱 앞에서만, 훅 3종·커서이동, 훅 0 이면 `⚠️훅 미수신`) | sysinput-win.js · main.js |
| **F-1** 설정 행 | 좁은 패널에서 버튼 세로 깨짐 | «프로그램 크기» · 부연 문구 제거 · `−`/`+` 28px · 줄바꿈 금지 | html · sim-ui-zoom |
| **제보 2** 가만히 있는데 쫓겨남 | 정원 초과 자기 퇴장이 memberId 정렬로 «늦게 온 사람» 을 가르는데 앞부분이 각자 PC 시계 | memberId = `_svNow()`(서버 보정) | firebase-init.js |
| **제보 4** 시크릿룸 방장 부여 안 됨 | 계정 이전 뒤 `friendCodes/{코드}` 가 옛 uid → 7일 게이트가 조용한 그 7일에 발급하면 `pub.owner` 가 옛 uid | 관리자 발급에 거울(`users/{uid}/friendCode`) 대조 경고 · 입장 시 owner ∈ 내가 버린 uid 면 «재발급 요청» 토스트(방장으로 쳐 주진 않음) | app.js |
| **제보 3** 두 PC 다중 접속 (+제보 ② 오프라인이 온라인으로) | presence 가 계정당 하나인데 두 기기가 각자 씀 | `users/{uid}/session` claim/lost/release. 밀린 기기: 방 퇴장 · presence onDisconnect 거둠 · `#deviceSessionOverlay`(«여기서 계속 쓰기» = 재claim) · startRoom 차단. **로그아웃 아님.** 로그인 계정만 | firebase-init.js · app.js · html · 규칙 |
| **제보 1** 플라잉체어 | 재생 방어는 전부 있음. `/80` ×3 깜짝쇼(기록 안 남김) 유력 | **보류.** 같은 제보 오면 «혼자 춤춘 사람 있었나」부터. 그때 ⓑ(터질 때 «💥 ○○님의 깜짝쇼» 한 줄) 권장 | — |
| 마이홈 BGM 60% | — | `bgmSliderVol`(원값) → `bgmVolume`(주입값) = home 이면 ×0.6. 주입부 7곳 무변경(sim-pl-loop 가 bgmForcePlay 를 오려 돌림) | main.js |
| 방 상한 250 | — | `ROOM_LIMITS` 워킹룸·투게더룸 250 · 표시 기본값 | app.js · html |

**되돌리지 말 것**
- 훅 수신 카운트는 `startGlobalHooks` 등록 자리에서 감싼다 — 핸들러 본문에 `_inputDiag` 를 두면 sim-wheel-kick 이 깨진다.
- `bgmForcePlay` 등 주입 문자열 안에 새 이름을 두지 않는다 — sim-pl-loop 의 무대에는 `bgmVolume` 만 있다.
- 시크릿룸 `_iAmSrOwner` 에 «내가 버린 uid» 를 넣지 않는다 — 방 전원이 `pub.owner` 문자열 하나로 같은 계산을 하므로 혼자 바꾸면 자기 퇴장 게이트가 어긋난다. 정정은 재발급(같은 코드 덮어쓰기)뿐.
- 기기 세션이 밀려도 `_loginDoLogout`/`_wipeAccountLocal` 을 부르지 않는다 — 원격 신호로 검문이 돌면 반쯤 남는 기기가 생긴다.
- `session` 규칙 `.validate` 는 `!newData.exists() ||` 로 시작해야 한다 — 없으면 로그아웃 때 삭제가 거부돼 다음 기기가 «남의 세션» 을 보고 헛되이 밀린다.

**배포 순서**: ① 규칙 게시(`firebase deploy --only database` 또는 콘솔) ② `npm run release`. 반대로 하면 그 사이 세션·슬롯 쓰기가 거부될 뿐 고장은 아니다.

---

## 4. 실기기에서 볼 것 (새 빌드)

| # | 무엇을 | 어떻게 갈리나 |
|---|---|---|
| E-2 | 관리자 권한 게임 앞에서 `[활성] … 등록=예 경로=없음(관리자권한)` + 마우스 움직이면 타이머 쌓임 | `[입력] … 클릭=0 키=0 커서이동>0 ⚠️훅 미수신` 이 찍히면 UIPI 갈래 실재 — 이번 수정으로 안 닫힘. 그때 «관리자 권한으로 실행» 안내 등 제품 결정 |
| F-1 | 설정 › 화면표시 두 행이 한 줄로 · 150% 에서 캐릭터·대화창 클릭이 정확한가 · F-2(팝업 절반) 남는가 | 좌표가 빗나가면 `[포커스]` 로그 좌표 ÷ 1.5 |
| C | 여백에 커서 두고 크롬 스크롤 → `kick3` 안 찍히고 스크롤 안 끊김 | |
| A | 로그아웃→재시작→로그인→재시작 뒤 레벨·가챠·캐릭터·플레이리스트·라이선스 그대로 · 친구 계정 전환 뒤 섞임 없음 · `users/` 에 임시 uid 노드 안 생김 | |
| 제보 2 | 방이 찬 상태에서 누가 들어와도 먼저 있던 사람이 안 쫓겨남 | |
| 제보 3 | PC-A 켜둔 채 PC-B 로그인 → A 차단 화면 · A 가 방에 있었으면 나감 · 친구 목록 온라인은 B 기준 · A «계속 쓰기» → B 차단 · 로그아웃 뒤 `users/{uid}/session` 삭제 | 규칙 미게시 빌드에서는 아무 화면도 안 떠야 정상 |
| 제보 4 | 관리자 발급에서 친구코드 넣었을 때 거울 불일치 경고가 뜨는 계정으로 시험 | |
| BGM | 마이홈 곡 = 슬라이더의 60% · 플레이리스트는 그대로 | |
| 슬롯·방 채널·날리기(09-16) | 앞 핸드오프들의 확인 항목 그대로 | |

---

## 5. 남은 것

- 규칙 게시 · 빌드 · 릴리즈 (§3 순서)
- 제보 1 보류 · 제보 ② 는 제보 3 으로 닫혔는지 확인
- E-2 UIPI 갈래가 실재하면 제품 결정
- `bug-triage-2026-09-15.md` C(4·5)·D(6) 는 09-16 에 닫혔는지 트리아지 문서 대조 — 6 은 A 로 닫힘
- CHECKS.md 기준선 줄 «초록 68 · 원본없음 1» 로 갱신(다음 개정 때)
- 맥 ⑦ electron 44 — 09-15 세션 이후 진행 상황은 `handoff-mac-runtime.md` 기준

---

## 6. 다음 세션에 올릴 것

이 파일 · `main.js` · `sysinput-win.js` · `app/parts/app.js` · `app/parts/firebase-init.js` · `app/desk-companion-prototype.html` · `preload.js` · `firebase-database-rules.json` · `checks/`(zip). ★ **저장소에서 꺼낸 것** — `f50980e` 판.
