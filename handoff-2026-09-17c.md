# 핸드오프 — 2026-09-17 (3차) · §3 전부 끝 · 커밋·릴리즈만 남음

앞 문서: `handoff-2026-09-17b.md`. 그 §3(폰트 · 알림음/서브메뉴)과 도중 제보(마이홈 서브메뉴)를 이 세션에서 끝냈다.
★ 남은 일은 **§1 커밋 → 릴리즈 한 번**뿐이다.

---

## 1. 커밋 · 릴리즈 (이 순서대로)

1. 이 세션 결과물을 저장소에 넣는다 (§2 표 — 세 번의 회신에 나눠 받았다. 마지막 판이 정본).
2. `node checks\run.js` → **검사 73개 · 초록 72 · 빨강 0 · 원본없음 1** 확인. 정본 대조가 «73개 전부 일치» 여야 한다.
3. `git status` — 앞 핸드오프 §2 의 미커밋분(날리기 회사원 게이트 · 꾸미기 파츠 표시)도 같이 들어가는지 확인.
4. `git add -A` → `git commit -m "feat: 채팅 알림음·글자 크기 서브메뉴 / fix: 한글 폰트(맑은 고딕)·마이홈 환경설정 서브메뉴"`
5. `package.json` 버전 — 0.9.6 이 아직 안 나갔으면 그대로, 나갔으면 0.9.7 로 올리고 커밋.
6. `npm run release`

## 2. 이 세션에서 바뀐 파일

| 저장소 경로 | 무엇 | 받은 회신 |
|---|---|---|
| `app/parts/myhome-desktop.js` | 환경설정 서브메뉴 `.fly` → `.mhd-fly` | 1차 |
| `app/desk-companion-prototype.html` | 한글 폰트 111곳 · 알림음/글자 크기 ▶ 메뉴 | 3차(최종) |
| `app/parts/app.js` | 한글 폰트 14곳 · 알림음 풀/설정/배선/수신 | 3차(최종) |
| `app/parts/mallang.js` | 한글 폰트 1곳 | 2차 |
| `app/parts/chat-notify-1.mp3` · `-2` · `-3` | 알림음 1(notify-2) · 알림음 2(notify-10) · 알림음 3(potion-drink-3) | 3차 |
| `checks/sim-mhd-envsub.js` | 신설 (§26) | 1차 |
| `checks/sim-font-hangul.js` | 신설 (§27) | 2차 |
| `checks/sim-chat-sfx.js` | 신설 (§28) | 3차 |
| `checks/CHECKS.md` | 개정 20 — 위 셋 등재 | 3차(최종) |

## 3. 릴리즈 노트 (0.9.6 «새 기능»/«고친 것» 에 추가)

- 대화창 설정에 **알림음**을 추가했습니다 — 소리 3종 중 고를 수 있고 ▷ 로 미리 들을 수 있어요. 대화창이 열려 있을 때는 울리지 않고, 회사원 모드에서는 «꺼짐»으로 표시되며 울리지 않습니다.
- 대화창 설정의 글자 크기가 **«글자 크기 ▶»** 서브메뉴로 정리됐습니다.
- (고침) 기본 테마와 머리 위 말풍선의 한글 글꼴이 예전(맑은 고딕)으로 돌아왔습니다.
- (고침) 마이홈 바탕화면 «환경 설정» 의 서브메뉴가 옆으로 흘러가던 문제를 고쳤습니다.

## 4. 원인 기록 (되돌리지 말 것)

- **한글 폰트** — Electron 31→44(9/15)로 한글 대체 폰트가 바뀌었다. Tahoma 목록마다 `Malgun Gothic` 을 Tahoma 바로 뒤에 둔다. 새 UI 는 `var(--win-font)` 를 쓰고, Tahoma 를 직접 적으면 한글 폰트를 붙일 것(sim-font-hangul).
- **`.fly` 는 전역**(채팅 날리기, flyAcross 애니메이션). 다른 UI 에 `fly` 클래스 이름을 쓰면 옆으로 흘러간다(sim-mhd-envsub).
- **알림음 풀은 prime 등록보다 먼저** 만든다. 울리는 자리는 수신 분기 한 곳뿐.
- **서브메뉴는 CSS :hover + left:100%** — 틈을 주면 깜빡인다.

## 5. 실기기에서 볼 것

- 알림음: 상대가 말하면 울림 · 대화창이 열려 있으면(최소화 포함) 안 울림 · 닫아 두면 울림 · 회사원 모드면 «꺼짐» 표시(흐림·«회사원 모드»)이고 안 울림 · 모드를 끄면 원래 선택으로 돌아옴 · 꺼짐이면 목록이 사라짐 · ▷ 미리듣기. 알림음 2(notify-10)는 0.3초짜리라 짧고, 알림음 1은 평균 음량이 낮다 — 크기가 들쭉날쭉하면 `CHAT_SFX_VOL` 이 아니라 파일을 노멀라이즈할 것.
- 글자 크기 ▶: 호버로 펼쳐짐 · 대화창을 화면 오른쪽 끝에 두면 왼쪽으로 펼쳐짐 · 버블 테마 모서리.
- 한글: 기본 테마 창 · 머리 위 말풍선 · 이름표가 대화창 글씨와 같은 모양인가.
- 마이홈 «환경 설정» › 글자 크기·브금 서브메뉴가 제자리에 뜨는가.
