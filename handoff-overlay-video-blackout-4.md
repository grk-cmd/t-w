# 핸드오프 4 — 동영상 검어짐/깜빡임 · 해결 후보

앞의 핸드오프 3 을 **대체한다.** 3 이 "원인 미확정" 상태로 넘긴 사안을, 이 세션에서
**기전을 문서로 확정하고** 후보를 다섯 개로 정리했다.

**이번 세션에서 코드에 손댄 것은 두 곳뿐이다.**
- `main.js` — `OVERLAY_GAP_PHYSICAL_PX` 6 → **12**
- `sim-overlay-gap.js` — 상수 개명으로 **죽어 있던 회귀 검사를 되살림**(아래 §7)


---

## ★ 닫음 (2026-09-14) — 잡은 것은 **레이어드 알파**였다

제보 증상이 사라졌다. 사용자 설정은 이렇다.

```json
{"overlayBottomGap":12,"overlayGapVer":2,"overlayLayeredAlpha":252}
```

**기전은 §1-4 의 「예외 목록」 그대로다.** `overlay-win.js:153` 이 인용해 둔 크로미움 판정:

```
if (flags & LWA_ALPHA && alpha < 255) return false;   // 알파가 255 미만이면 제외
```

알파 252 면 `IsWindowVisibleAndFullyOpaque` 가 거짓이 되어 **우리 창이 「가릴 수 있는 창」
후보에서 통째로 빠진다.** 갭처럼 사각형을 몇 px 비껴가는 것이 아니라 **겹침 계산 자체가
일어나지 않는다** — 그래서 훨씬 강하다.

★ **§1 이 「아직 안 풀린 것」으로 남긴 것도 이걸로 설명된다.** 「브라우저 창모드에서는 우리 창
안에 통째로 들어가므로 재발해야 하는데 실측은 정상이었다」 — 기하가 애초에 상관없기 때문이다.

### 무엇을 그대로 두기로 했나 (사용자 결정 · 2026-09-14)

- **갭 12 유지.** 알파가 잡은 것이라 갭은 이제 대가만 남는다(화면 맨 아래 물리 12px 에서
  캐릭터 판정 안 됨). 그래도 되돌리지 않는다 — 되돌리려면 설정 파일 `overlayBottomGap` 한 줄이다.
- **알파 기본값은 꺼짐(0) 그대로.** `overlay-win.js:195` 가 기본 0 이고, 252 는 토글을 켠 값이다.
  2026-08-26 에 일부러 되돌린 결정이고(⚠️ `electron#40515` 계열 — 투명 창에 `LWA_ALPHA` 를
  거는 조합이 일부 GPU 에서 **우리 창**을 검게 만든다), 그 근거는 그대로 유효하다.

### ⚠️ 그래서 남는 것 하나 — 제보자에게는 아직 꺼져 있다

기본이 꺼짐이므로 **새 빌드를 받아도 토글을 켜지 않으면 제보자 PC 에서는 그대로다.**
같은 제보가 또 오면 **먼저 이것부터 시킬 것** — 빌드가 필요 없다:

```
%APPDATA%/Together Working/tw-settings.json  →  "overlayLayeredAlpha": 252
```

여러 명에게서 듣고 부작용 보고가 없으면 그때 기본값을 252 로 올릴지 정한다.
**지금 올리지 않는다** — `electron#40515` 위험을 되안는 일이고, 근거가 아직 한 사람이다.

### 안 쓴 후보 넷 — 폐기가 아니라 **불필요**

§4 의 `skipTaskbar` · `setConfigMode` 억제 · 2창 구조 · C안 토글은 **손대지 않았고 필요도 없었다.**
다만 §1 의 기전 자체는 살아 있다 — 크로미움 가려짐 판정은 **맥에도 있다.** ⑥단계(`overlay-mac.js`)
에서 같은 사안을 다시 만나고, 그쪽엔 `WS_EX_LAYERED` 도 `LWA_ALPHA` 도 없다.
**이 문서의 §1 은 그때 다시 읽을 것.**

⚠️ §4-2(`setConfigMode` 반복 호출로 z-order 흔들기)는 **확인 안 한 채 닫는다.**
제보자 로그의 `[오버레이] run 진입` 줄 개수를 세지 않았다. 증상이 사라져 급하지 않지만,
그 자리는 `fs.appendFileSync` 를 반복해 로그 상한을 채우는 곁다리 피해가 따로 있다(§4-2).
「깜빡임」 제보가 다시 오면 **거기부터 볼 것.**

---

## 0. 먼저 읽을 것

- **§1 은 확정 사실이다.** 크로미움 공식 문서가 기전을 그대로 적어 놓았다. 추측이 아니다.
- **§2 는 실측이다.** 제보 영상 프레임을 재서 나온 숫자다.
- **§4 의 후보는 전부 가설이다.** 12 로 올린 것은 문턱을 찾은 게 아니라 **여유를 준 것**이다.
- ⚠️ 이 사안은 추론으로 원인을 좁히려는 시도가 **네 번 연달아 틀렸다.** §1 이 생긴 지금도
  "그러니 A 일 것이다"로 건너뛰지 말 것. 갈래는 반드시 실측으로 가른다.

---

## 1. 기전 — **확정.** 크로미움의 가려짐(occlusion) 판정이다

근거: `chromium/docs/windows_native_window_occlusion_tracking.md` (공식 문서)

1. 크로미움은 자기 창이 다른 창에 완전히 덮이면 **포그라운드 탭을 백그라운드 탭처럼** 다룬다 —
   렌더링을 멈추고 JS 를 스로틀한다.
2. 판정 방식: 가상 화면 전체를 "안 가려진 영역"으로 두고, **z순서 위에서부터 각 창의 사각형을
   그 영역에서 뺀다.** 대상 창의 사각형을 빼도 영역이 안 변하면 = 가려진 것.
3. ★ 문서에 이렇게 적혀 있다 — **가려졌다고 잘못 판정되면 콘텐츠 영역이 흰색이 된다.**
4. ★ 그리고 예외 목록: **투명(transparent) 창 · cloaked 창 · floating 창 · 비사각형 창은
   가리는 창으로 치지 않는다.**

### 이것이 지금까지의 실측을 전부 설명한다

| 관측 | 설명 |
|---|---|
| 통과 중(ignore=true)이면 정상 | `setIgnoreMouseEvents(true)` = `WS_EX_TRANSPARENT` → **4번의 "투명 창" 예외**에 걸려 가리는 창이 아니게 된다 |
| 클릭 받는 중(ignore=false)이면 블라인드 | 예외가 벗겨져 가리는 창이 된다 → 브라우저가 완전 차폐 판정 |
| 몇 px 짧게 하면 정상 | 2번의 "사각형 빼기"에서 영역이 변하므로 차폐가 성립하지 않는다 |
| 흰색/검정 두 가지 | 3번 그대로(다크모드 여부) |
| 페이지는 멀쩡한데 영상만 빈다 | 렌더링이 멈추면 **마지막 페이지 프레임은 화면에 남고**, 별도 평면으로 계속 갱신되던 영상만 사라진다 |

⚠️ **아직 안 풀린 것:** 브라우저 창모드에서는 우리 창 안에 통째로 들어가므로 재발해야 하는데
실측은 정상이었다. §1 이 전부는 아니라는 뜻이다 — 그래서 §4 의 후보를 남긴다.

### 같은 사례 (다시 조사하지 말 것)

| 출처 | 내용 |
|---|---|
| electron#49024 | 투명 창이 크롬 창을 완전히 덮으면 영상이 안 나온다. repro gist 있음 |
| electron#30798 | 전체화면 투명 창이 흰색. **좌상단을 1px 옮기면 사라짐.** 메인테이너 재현 확정 |
| electron#27286 | 전체화면 투명 창이 불투명. 우회법이 **width·height 에서 2px 빼기** |
| WindowTop #146·#117 | 크롬이 영상 재생을 멈춤. 개발자가 결국 "크롬을 속이는" 방법을 택함 |
| OnTopReplica #171 | 유튜브 영상 멈춤. 최신 크롬에서 플래그가 사라져 기존 해법이 안 듣는다 |
| CEF #3638 | `--disable-features=CalculateNativeWinOcclusion` 으로 해결 |
| 2019 크롬 흰 화면 대란 | 같은 기능이 stable 에 켜져 Citrix 환경이 통째로 흰 화면 |

★ **`CalculateNativeWinOcclusion` 플래그는 우리 해법이 될 수 없다.** 브라우저 쪽에 붙여야 듣고,
최신 크롬에서는 목록에서 사라졌다는 보고까지 있다. 사용자 전원에게 시킬 수 없다.

---

## 2. 지금 무엇이 남아 있는가 — 제보 영상 실측 (2026-08-24)

영상 프레임을 5fps 로 떠서 영상 영역의 밝기·표준편차를 쟀다.

- 정상 구간: 평균 105 / 표준편차 33
- **빈 구간: 평균 190~210 / 표준편차 2** (완전 백색)
- 타임라인: 3.8초(1프레임) → 5.0~7.4초(2.6초) → 8.8초~끝(1.4초 이상)

⇒ 유저가 말한 "렉·버퍼링·깜빡임"은 **기존 블라인드가 짧게 끊어져 반복되는 것**이다.
   새 증상이 아니라 **덜 고쳐진 상태**다.

⚠️ 단, 이 제보자 빌드에는 **물리픽셀 개정판이 안 들어가 있었다**(값 2 DIP 시절).
   진단 로그(`tw-mouse-diag.log`)에 `[오버레이] run 진입` 과 `[포커스]` 줄이 **0줄**인 것이 증거다.
   ⇒ **12 를 내보낸 뒤 다시 받아야 한다.** 지금 로그로는 아무것도 못 가른다.

---

## 3. 이번에 한 것 — 갭 6 → 12

`main.js:83  OVERLAY_GAP_PHYSICAL_PX = 12`

실측으로 확정된 점은 둘뿐이다.

| 환경 | 실제 틈 | 결과 |
|---|---|---|
| 4K·150% | 물리 3px | 정상 |
| QHD 제보자 | 물리 2~3px | **재발** |

**둘이 거의 붙어 있다.** 문턱이 그 사이 어딘가라는 것만 알고, 6 이 안전한지는 실기기에서
확인된 적이 없다. 그래서 문턱 탐색은 미루고 폭을 두 배로 벌렸다.

- 대가: 화면 맨 아래 물리 12px 에서 캐릭터 판정이 안 된다(작업표시줄 바로 윗줄).
  100% 배율에서 캐릭터가 6px 위로 올라온다 — 릴리즈 전에 눈으로 한 번 볼 것.
- 되돌리는 길: 설정 파일의 `overlayBottomGap` — **빌드 없이** 제보자에게 값만 바꾸게 할 수 있다.
- ⚠️ **12 로도 제보가 오면 숫자를 더 키우지 말 것.** 그건 이 길이 틀렸다는 신호이고,
  §4 의 다른 후보로 넘어가야 한다는 뜻이다. 물리 32px 을 넘겨야 한다면 확실히 아니다.

---

## 4. 남은 후보 — **전부 가설. 실측으로 가를 것**

### 4-1. ★ `WS_EX_TOOLWINDOW` (가장 유력 · 미검증)

§1 의 4번 "floating 창은 가리는 창으로 치지 않는다"의 **floating 이 무엇인지**가
Mozilla 의 포팅 소스(`WinWindowOcclusionTracker.cpp`, 크로미움 로직을 그대로 옮긴 것)에
코드로 남아 있다 — `WS_EX_TOOLWINDOW` 가 붙은 창은 후보에서 걸러내되 작업표시줄만 예외.

그리고 Electron 에서 이 스타일을 세우는 옵션이 **`skipTaskbar: true`** 다.
우리 `mainWindow` 는 `main.js:810` 에서 **`skipTaskbar: false`** — 즉 지금 우리 오버레이는
"가릴 수 있는 창" 후보에 **들어가 있다.**

- **장점**: 창 크기·좌표·카메라·`deskPan` 을 한 줄도 안 건드린다. 갭도 필요 없어진다.
- ⚠️ **대가가 크다** — 작업표시줄 버튼이 사라지면 **`flashFrame`(초대 알림 깜빡임)이 죽는다.**
  preload 주석이 짝 호출을 강하게 못박아 둔 기능이라 그냥 못 바꾼다.
- ⚠️ 크로미움 최신 원본이 아니라 Mozilla 포팅본으로 확인한 것이다. 원본 확인이 먼저다.
- **실험**: run 모드에서 `mainWindow.setSkipTaskbar(true)` 를 임시로 걸고 영상 위에서 확인.
  한 줄이면 되고, 되돌리기도 한 줄이다. **갭을 0 으로 낮춘 상태에서 해야 의미가 있다** —
  안 그러면 갭이 이미 막고 있어서 무엇 덕분인지 못 가른다.

### 4-2. `setConfigMode` 반복 호출 — z-order 흔들기 (미검증)

`main.js:1828` 의 `companion:setConfigMode` 핸들러는 부를 때마다 **조건 없이**
`setBounds(...)` + `setAlwaysOnTop(true,'screen-saver')` 를 낸다. 렌더러 쪽
`applyDesktopRunClass`(app.js:31192)도 조건 없이 이 IPC 를 보내고, 그 함수는
`layoutSeats` 래퍼 끝에 붙어 있으며 **`layoutSeats` 호출부는 38곳 + resize 리스너**다.

§1 문서에 <창 이동·리사이즈·최소화/복원·포그라운드 변경 이벤트에 훅을 걸고 16ms 뒤 재계산>
이라고 되어 있다. 즉 우리가 그 훅을 계속 때리고 있고, 재계산 결과가 매번 흔들리면 **깜빡임**이 된다.

곁다리 피해가 둘 더 있다.
- `_logOverlayGeometry` → `_diagLog` → **`fs.appendFileSync`** — 메인 프로세스 동기 디스크 쓰기가 반복된다.
- 그 줄이 로그 256KB 상한을 채워 **정작 필요한 클릭고착 기록을 밀어낸다.**

- **실험**: 12 를 내보낸 뒤 받은 로그에서 `[오버레이] run 진입` **줄 개수**를 센다.
  수백 줄이면 확정. 대응은 핸들러 앞에 "직전과 같은 rect·같은 모드면 아무것도 안 함" 한 줄.

### 4-3. `display-metrics-changed` 미처리 (작은 구멍, 확정 사실)

`main.js` 에 이 핸들러가 **없다.** 해상도·배율·작업영역이 바뀌어도 틈을 다시 안 잡는다.
다음 `layoutSeats` 때 우연히 복구되는 구조라 "어떨 때는 되고 어떨 때는 안 된다"의 재료가 된다.
갭 갈래로 가기로 확정되면 이건 반드시 막을 것.

### 4-4. 2창 구조 (핸드오프 3 §6-1 — 보류 유지)

큰 창은 통과 고정, 클릭은 캐릭터 크기의 작은 창이 받는다. §1 로 보면 **원리적으로 확실히 듣는다**
(큰 창이 늘 `WS_EX_TRANSPARENT` 라 영영 가리는 창이 아니다).
⚠️ 오버레이가 포커스를 못 얻어 **채팅·닉네임 등 텍스트 입력이 죽는 문제**를 따로 풀어야 한다.
지금 갭으로 되고 있으므로 후순위.

### 4-5. C안 — "영상 볼 때 방해 안 함" 토글 (임시 회피)

켜면 캐릭터를 숨기거나 통과 고정. **모드 A 가 곧 이 동작이고 실측으로 영상이 멀쩡했다.**
하루면 되고 어느 갈래든 값싸다. 잃는 것은 그 시간 동안 캐릭터를 못 만지는 것뿐.

---

## 5. ⓕ(파판14 대응)는 이 사안을 **못 잡는다** — 검증 완료

`_guardStaleCapture` 를 원본 그대로 떼어내 돌렸다(`sim-video-blank-window.js`).

| 시나리오 | ⓕ 발동 | 영상이 비는 시간 |
|---|---|---|
| 파판14(게임 안에서 커서를 크게 휘저음) | ✅ | 1.5초 뒤 회수 — **대응은 유효** |
| **영상 보는 중, 커서 정지** | ❌ | **계속**(20초 돌려도 안 풀림) |
| 영상 보는 중, 커서 조금씩(20px/500ms) | ❌ | 계속 |
| 커서가 캐릭터·상태칩 위 (정상 `ignore=false`) | ❌(개입 안 함이 맞음) | 그동안 계속 |

발동 조건 5번이 **"500ms 안에 커서가 60px 이상 움직였을 것"** 이다. 영상은 마우스를 놓고 본다.
게다가 ⓕ 는 예방이 아니라 **사후 회수**라 발동해도 최소 1.5초는 이미 비어 있다.
⇒ **ⓕ 는 파판14 건 그대로 두고, 이 사안과 엮지 말 것.**

---

## 6. 반복 금지 (핸드오프 3 §9 를 그대로 승계 + 추가)

| 시도 | 결과 |
|---|---|
| `disable-direct-composition` | 효과 없음. 우리 프로세스에만 적용된다 |
| `disable-gpu-compositing` | 증상은 잡히나 앱이 매우 무거워짐 |
| `disable-features=EnableTransparentHwndEnlargement` | 효과 없음 |
| `FPS_UNFOCUSED` 조정 | 모드 E·F 로 최종 확인. 프레임 수·오르내림 전부 무관 |
| `--disable-gpu` / `disableHardwareAcceleration()` | 방향이 반대다. three.js 가 소프트웨어로 떨어진다 |
| 프레임워크 교체(Tauri 등) | 같은 Win32 API 다. Tauri 엔 `forward:true` 가 없다 |
| **`--disable-features=CalculateNativeWinOcclusion`** | **우리 프로세스엔 무의미.** 브라우저에 붙여야 듣고 최신 크롬엔 플래그가 없다 |
| **`_uiHot` 을 hover 에서 다시 켜기** | 그게 깜빡임 제보의 원인이었다(app.js:31515 주석). 되살리지 말 것 |

---

## 7. 회귀 검사 — `sim-overlay-gap.js` 를 되살렸다

**그동안 한 항목도 안 돌고 있었다.** 상수가 `OVERLAY_BOTTOM_GAP`(DIP) →
`OVERLAY_GAP_PHYSICAL_PX`(물리 픽셀) 로 개명되면서 슬라이스가 심볼을 못 찾고 `exit(1)` 했다.
화면에는 ✗ 한 줄만 뜨는데 그게 "검사 실패"가 아니라 "검사를 못 했다"는 뜻이라 눈으로 구분이 안 됐다.

지금은 24항목 전부 통과한다. 새로 넣은 것:
- **배율 1 / 1.25 / 1.5 / 1.75 / 2 / 2.5 각각에서 물리 픽셀이 목표치 이상인가** — 이게 이 수정의 핵심이다
- 목표 틈이 실측 하한(물리 3px)보다 여유가 있는가
- `overlayBottomGap`(설정 파일 통로)과 `_logOverlayGeometry`(진단 로그)가 살아 있는가

⇒ **상수를 또 개명하면 이 파일도 같이 볼 것.** 회귀 검사가 조용히 죽는 것이 제일 나쁘다.

---

## 8. 다음 사람이 할 일 — 순서대로

1. **12 로 릴리즈**하고, 제보자에게 `%APPDATA%/Together Working/tw-mouse-diag.log` 를 다시 받는다.
2. 그 로그에서 **두 가지만** 본다.
   - `[오버레이] run 진입` 의 `아래틈 물리 Npx` → 12 인가? 0 이면 틈 자체가 안 생긴 것이다.
   - `[오버레이] run 진입` **줄 개수** → 수백 줄이면 §4-2 확정.
3. 틈이 12 인데도 증상이 나면 → `overlayBottomGap` 을 24 로 넣어 보게 한다(빌드 불필요).
   그래도 나면 **갭 갈래는 끝이다.** §4-1 실험으로 넘어간다.
4. 증상이 사라지면 12 → 8 → 6 으로 내리며 문턱을 찾는다(전부 설정 파일로 가능).

⚠️ **눈으로만 확인되는 사안이다.** 겹침·합성은 노드 검사로 못 잡는다.
   sim 으로 잡을 수 있는 것은 갈래가 정해진 뒤의 산수뿐이다.
